# Copy to .env and fill in. NEVER commit .env.
# The studio tools read this file from the repo root (or ~/.studio.env).

# --- Synology NAS (DS423) ---
NAS_HOST=192.168.68.131
NAS_DSM_PORT=5001            # HTTPS DSM Web API
NAS_USER=tomj12k646
NAS_PASS=CHANGE_ME
NAS_SMB_HOST=192.168.68.131

# --- DGX Spark ---
SPARK_HOST=spark-d1a9.local
SPARK_IP=192.168.68.136
SPARK_USER=pizzacat
# SSH uses key auth (deploy/spark/install_ssh_key.sh). Password only for bootstrap.
SPARK_PASS=CHANGE_ME

# --- Service endpoints on the Spark ---
VLLM_URL=http://spark-d1a9.local:8000/v1
VLLM_MODEL=Qwen3.8-27B
COMFYUI_URL=http://spark-d1a9.local:8188
RESOLVE_DB_HOST=spark-d1a9.local
RESOLVE_DB_PORT=5432
RESOLVE_DB_USER=resolve
RESOLVE_DB_PASS=CHANGE_ME
